import { EspecialidadFilterPipe } from './especialidad-filter.pipe';

describe('EspecialidadFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EspecialidadFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
